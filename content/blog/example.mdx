---
title: Blog
description: Blog for the project
published: 2025-09-09
---

Hello world!

```tsx lineNumbers title="Hello World"
export async function CodeBlock({
  children,
  lang,
  ...rest
}: HTMLAttributes<HTMLElement> & {
  children: string
  lang: string
}) {
  const rendered = await highlight(children, {
    lang,
    theme: "vesper",
    components: {
      pre: (props) => <Pre {...props} />,
    },
  })

  return <CodeBlockClient {...rest}>{rendered}</CodeBlockClient>
}
```

```tsx tab="Tab 1" title="Hello World"
export async function CodeBlock({
  children,
  lang,
  ...rest
}: HTMLAttributes<HTMLElement> & {
  children: string
  lang: string
}) {
  const rendered = await highlight(children, {
    lang,
    theme: "vesper",
    components: {
      pre: (props) => <Pre {...props} />,
    },
  })

  return <CodeBlockClient {...rest}>{rendered}</CodeBlockClient>
}
```

```css tab="Tab 2" title="Testing"
console.log('B');
```